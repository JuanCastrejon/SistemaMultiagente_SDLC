# Issue Tracker

GitHub Issues es el tracker por defecto.

## Reglas

- Los drafts locales viven en `.github/agent-state/issues/local/<slug>/`.
- `publish-trace.ps1` publica issues solo con `-Apply`.
- Cada issue funcional debe incluir fase, readiness y superficie.
